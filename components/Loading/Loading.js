import React from 'react';

import './Loading.css';

const Loading = (props) => (
  <div className="loader mx-auto d-block">
    <div />
    <div />
    <div />
    <div />
  </div>
);

export default Loading;